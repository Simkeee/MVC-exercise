import "@picocss/pico";
import "./style.css";
import { init } from "./app/ctrl";

window.addEventListener("DOMContentLoaded", () => {
  init();
});

window.addEventListener("DOMContentLoaded", () => {
  init();
});
